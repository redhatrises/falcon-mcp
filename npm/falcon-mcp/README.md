# falcon-mcp (npm)

    npx falcon-mcp --help

Installs the CrowdStrike Falcon MCP server. The correct platform binary is
delivered as an optional dependency and executed directly — no runtime download.
